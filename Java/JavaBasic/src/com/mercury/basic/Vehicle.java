package com.mercury.basic;

// all functions in interface are public abstract ( <= Java 1.7)
// all fields in interface are public static final by default
public interface Vehicle {

	int X = 3;
	void run();
}
